<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="payment_transaction_form" model="ir.ui.view">
        <field name="name">payment.transaction.form</field>
        <field name="model">payment.transaction</field>
        <field name="inherit_id" ref="payment.payment_transaction_form"/>
        <field name="arch" type="xml">
            <header>
                    <button type="object" name="mp_payment_order_get" attrs="{'invisible':[('provider_code', '!=', 'mercado_pago_qr')]}" string="Check order status" class="oe_highlight"/>
                    <button type="object" name="action_mp_open_qr" attrs="{'invisible':[('provider_code', '!=', 'mercado_pago_qr')]}" string="Open QR"/>
            </header>
        </field>
    </record>

</odoo>